import cloudbeds_api
import seam_api

#def handle_cb_callback():
#handle the callback request when a new property is added


#def reservation_checkin(): 
    #action to take when a reservation is checked in

#def write_mapping(property_id, mapping):
#write the mapping for 

#def load_mapping(property_id):
# load the mapping for 
